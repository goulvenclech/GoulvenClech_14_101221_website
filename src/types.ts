/**
 * All types used in our App
 */
export interface Employee {
  firstName: string,
  lastName: string,
  startDate: string,
  departement: string,
  dateOfBirth: string,
  street: string,
  city: string,
  state: string,
  zipCode: string
}